steps:
- name: Starting measurement for lb-consistent-header-subset application
  measurements:
  - Method: WaitForControlledPodsRunning
    Instances:
    - Identifier: WaitForRunningDeployments
      Params:
        apiVersion: apps/v1
        kind: Deployment
    Params:
      action: start
      labelSelector: group = lb-consistent-header-subset
      operationTimeout: "120s"

- name: Create prism-conf Config
  phases:
  - namespaceRange:
      min: 30
      max: 30
    replicasPerNamespace: 1
    tuningSet: {{.tuningSet}}
    objectBundle:
    - basename: prism-conf
      objectTemplatePath: modules/lb-consistent-header-subset/prism-conf.yaml

- name: Create lb-consistent-header-subset service and deploy
  phases:
  - namespaceRange:
      min: 30
      max: 30
    replicasPerNamespace: 1
    tuningSet: {{.tuningSet}}
    objectBundle:
    - basename: mockserver
      objectTemplatePath: modules/lb-consistent-header-subset/mockserver-svc.yaml
    - basename: mockserver-v1-deploy
      objectTemplatePath: modules/lb-consistent-header-subset/mockserver-v1-deploy.yaml
    - basename: mockserver-v2-deploy
      objectTemplatePath: modules/lb-consistent-header-subset/mockserver-v2-deploy.yaml
    - basename: mockserver-v3-deploy
      objectTemplatePath: modules/lb-consistent-header-subset/mockserver-v3-deploy.yaml
    - basename: mockserver-v4-deploy
      objectTemplatePath: modules/lb-consistent-header-subset/mockserver-v4-deploy.yaml

- name: Waiting for wordpress application setup to be completed
  measurements:
  - Method: WaitForControlledPodsRunning
    Instances:
    - Identifier: WaitForRunningDeployments
    Params:
      action: gather
