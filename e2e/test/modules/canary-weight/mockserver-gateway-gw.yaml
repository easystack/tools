apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: mockserver-gateway
spec:
  selector:
    istio: istio-ingressgateway
  servers:
  - hosts:
    - mockserver
    - mockserver.com
    - www.mockserver.com
    port:
      name: http
      number: 80
      protocol: HTTP
