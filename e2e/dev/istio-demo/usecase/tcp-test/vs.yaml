apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: mockserver
spec:
  gateways:
  - mesh
  hosts:
  - mockserver
  tcp:
  - route:
    - destination:
        host: mockserver
        port:
          number: 80
