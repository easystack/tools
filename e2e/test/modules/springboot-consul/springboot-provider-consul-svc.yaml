apiVersion: v1
kind: Service
metadata:
  labels:
    app: springboot-provider
  name: springboot-provider-consul
spec:
  ports:
    - name: http
      port: 8082
      protocol: TCP
      targetPort: 8082
  selector:
    app: springboot-provider